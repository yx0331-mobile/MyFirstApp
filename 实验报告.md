# 实验1 Android开发基础

## 一、实验目的

1. 熟悉 Android Studio 的安装与基本配置
2. 掌握创建 Android 工程的基本流程
3. 学习将 Android 项目同步至 Gitee/GitHub 远程仓库

## 二、实验环境

| 项目 | 版本/路径 |
|------|-----------|
| 操作系统 | Windows |
| Android Studio | 2026.1.3 |
| JDK | 21（Embedded JBR） |
| Android SDK | API 37 |
| 构建工具 | Gradle 9.5.0 |
| 开发语言 | Java |
| 远程仓库 | GitHub（https://github.com/yx0331-mobile/MyFirstApp） |
| Git | C:\Program Files\Git\bin\git.exe |

## 三、实验内容与步骤

### 3.1 安装 Android Studio

1. 从官网下载 Android Studio 安装包
2. 运行安装程序，选择安装路径，完成安装
3. 首次启动时，配置 SDK 路径（`D:\Eason\Android_data`），Studio 会自动下载 SDK 组件

> **注意事项**：国内网络环境下，首次启动下载 SDK 可能较慢，需耐心等待。

### 3.2 创建第一个 Android 工程

1. 打开 Android Studio，点击 **New Project**
2. 选择模板：**Phone and Tablet → Empty Views Activity**（带 Views，使用传统 View 体系）
3. 配置项目信息：

| 配置项 | 值 |
|--------|-----|
| Project Name | MyFirstApp |
| Package Name | com.example.myfirstapp |
| Save Location | D:\Eason\AndroidProjects\MyFirstApp |
| Language | Java |
| Minimum SDK | API 24（Android 7.0） |
| Build configuration | API 37 |

4. 点击 **Finish**，等待 Gradle 同步完成

### 3.3 解决 Gradle 同步问题

由于国内网络环境，默认的 Gradle 下载地址和 Maven 仓库无法正常访问，需要更换镜像源。

#### 3.3.1 更换 Gradle 下载镜像

修改 `gradle/wrapper/gradle-wrapper.properties`：

```properties
# 修改前
distributionUrl=https\://services.gradle.org/distributions/gradle-9.5.0-bin.zip

# 修改后（腾讯云镜像）
distributionUrl=https\://mirrors.cloud.tencent.com/gradle/gradle-9.5.0-bin.zip
```

#### 3.3.2 更换 Maven 仓库镜像

修改 `settings.gradle.kts`，在 `pluginManagement` 和 `dependencyResolutionManagement` 的 repositories 中添加阿里云镜像：

```kotlin
pluginManagement {
    repositories {
        maven { url = uri("https://maven.aliyun.com/repository/google") }
        maven { url = uri("https://maven.aliyun.com/repository/public") }
        maven { url = uri("https://maven.aliyun.com/repository/gradle-plugin") }
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven { url = uri("https://maven.aliyun.com/repository/google") }
        maven { url = uri("https://maven.aliyun.com/repository/public") }
        google()
        mavenCentral()
    }
}
```

#### 3.3.3 配置 JDK

1. 打开 `File → Project Structure → SDK Location`
2. JDK location 选择 **Embedded JDK**（Android Studio 自带的 JBR，版本为 JDK 21）
3. 点击 Apply → OK

#### 3.3.4 重新同步

点击工具栏的大象图标 **Sync Project with Gradle Files**，Gradle 同步成功：

```
BUILD SUCCESSFUL in 5m 10s
33 actionable tasks: 33 executed
```

### 3.4 配置 .gitignore

Android 项目需要忽略编译产物、IDE 配置、本地 SDK 路径等文件。根目录 `.gitignore` 配置如下：

```gitignore
# Built application files
*.apk
*.aab
*.dex

# Gradle files
.gradle/
build/

# Local configuration file
local.properties

# Android Studio generated files
.idea/
*.iml

# External Native build
.externalNativeBuild/
.cxx/

# OS files
.DS_Store
Thumbs.db

# Log files
*.log

# Package Files
*.jar
*.zip
```

### 3.5 同步至 GitHub 远程仓库

1. **注册 GitHub 账号**：使用邮箱 2737760282@qq.com 注册 GitHub 账号，用户名 yx0331-mobile
2. **创建 Personal Access Token**：GitHub 不支持 HTTPS 密码推送，需要创建 Token（Settings → Developer settings → Personal access tokens → Generate new token (classic)，勾选 repo 权限）
3. **创建远程仓库**：在 GitHub 上新建空仓库 `MyFirstApp`，不勾选任何初始化选项
4. **初始化本地 Git 仓库**：
   ```powershell
   cd D:\Eason\AndroidProjects\MyFirstApp
   git init
   ```
5. **添加文件并提交**：
   ```powershell
   git add -A
   git commit -m "feat: first Android project - MyFirstApp"
   ```
6. **关联远程仓库并推送**：
   ```powershell
   git remote add origin https://github.com/yx0331-mobile/MyFirstApp.git
   git push -u origin master
   # 推送时输入 GitHub 用户名和 Personal Access Token（代替密码）
   ```

推送成功，输出如下：

```
branch 'master' set up to track 'origin/master'.
To https://github.com/yx0331-mobile/MyFirstApp.git
 * [new branch]      master -> master
```

## 四、项目结构

```
MyFirstApp/
├── app/
│   ├── build.gradle.kts
│   ├── .gitignore
│   └── src/
│       ├── androidTest/          # 自动化测试代码
│       ├── main/
│       │   ├── AndroidManifest.xml    # 应用清单文件
│       │   ├── java/com/example/myfirstapp/
│       │   │   └── MainActivity.java  # 主活动
│       │   └── res/                   # 资源文件
│       │       ├── drawable/          # 图片资源
│       │       ├── layout/             # 布局文件
│       │       ├── mipmap-*/           # 应用图标
│       │       └── values/             # 字符串、颜色、主题
│       └── test/                  # 单元测试代码
├── gradle/
│   ├── libs.versions.toml         # 依赖版本管理
│   └── wrapper/                   # Gradle Wrapper
├── build.gradle.kts               # 根级构建脚本
├── settings.gradle.kts           # 项目设置
├── gradle.properties              # Gradle 属性配置
├── gradlew / gradlew.bat          # Gradle Wrapper 脚本
└── .gitignore                     # Git 忽略规则
```

## 五、核心代码

### 5.1 MainActivity.java

```java
package com.example.myfirstapp;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.view.WindowCompat;
import androidx.core.view.WindowInsetsCompat;
import androidx.core.view.WindowInsetsControllerCompat;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        WindowCompat.setDecorFitsSystemWindows(getWindow(), false);
        setContentView(R.layout.activity_main);
    }
}
```

### 5.2 AndroidManifest.xml

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFirstApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

### 5.3 activity_main.xml

```xml
<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
```

## 六、实验结果

1. **Android Studio 安装成功**，版本 2026.1.3，SDK 配置完成
2. **项目创建成功**，Empty Views Activity 模板，Java 语言，编译通过
3. **Gradle 同步成功**，BUILD SUCCESSFUL，33 个任务全部完成
4. **GitHub 远程仓库推送成功**，仓库地址：https://github.com/yx0331-mobile/MyFirstApp
5. **.gitignore 配置正确**，本地临时文件未被提交

## 七、问题与解决

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| Gradle sync failed，UnknownHostException | 国内网络无法访问 services.gradle.org 和 maven.google.com | 更换为腾讯云 Gradle 镜像和阿里云 Maven 镜像 |
| Project JDK is not defined | 未配置 JDK 路径 | 在 Project Structure 中选择 Embedded JDK |
| GitHub 网页无法打开（机器人验证） | 代理 IP 被 GitHub 反爬虫机制拉黑 | 关闭代理或更换代理节点，最终使用浏览器自动化工具成功访问 |
| GitHub SSH 连接被拒（端口 22 关闭） | 代理拦截了 SSH 协议的 22 端口 | 改用 HTTPS + Personal Access Token 方式推送 |
| Gitee 仓库 404 not found | 仓库名大小写不匹配，Gitee 自动转为小写加短横线 | 使用 `my-first-app` 而非 `MyFirstApp` |

## 八、实验心得

通过本次实验，完成了 Android 开发环境的搭建和第一个工程的创建。实验过程中遇到了国内网络环境下 Gradle 无法正常下载的问题，通过更换国内镜像源得以解决。这让我意识到在国内进行 Android 开发，配置合适的镜像源是必要的前置步骤。同时，正确配置 `.gitignore` 对于保持远程仓库整洁也非常重要。实验为后续 Android 开发课程打下了基础。

---

# 实验2 Android界面布局

## 一、实验目的

1. 学习 Android 官方文档中关于 `ConstraintLayout`、`LinearLayout` 和 `TableLayout` 的布局知识
2. 利用线性布局（LinearLayout）实现 4×4 网格表格界面
3. 利用表格布局（TableLayout）实现菜单列表界面
4. 利用约束布局（ConstraintLayout）实现计算器界面
5. 利用约束布局（ConstraintLayout）实现太空旅行规划界面
6. 使用 Jetpack Compose 实现任务列表界面，掌握声明式 UI 编写方法

## 二、实验环境

| 项目 | 版本/路径 |
|------|-----------|
| 操作系统 | Windows 11 |
| Android Studio | 2026.1.3 |
| JDK | 21（Embedded JBR） |
| Android SDK | API 37 |
| 构建工具 | Gradle 9.5.0、AGP 9.3.0 |
| 开发语言 | Java + Kotlin（Compose） |
| Kotlin | 2.0.0 |
| Compose BOM | 2024.09.00 |
| 远程仓库 | GitHub（https://github.com/yx0331-mobile/MyFirstApp） |

## 三、实验内容与步骤

### 3.1 学习官方文档

学习 Android 官方布局文档：`https://developer.android.google.cn/guide/topics/ui/declaring-layout.html`，重点了解 `ConstraintLayout`、`LinearLayout` 和 `TableLayout` 三种布局的特点和使用方法。

### 3.2 项目配置

在实验一的 MyFirstApp 项目基础上，修改构建配置以启用 Kotlin 和 Jetpack Compose：

1. 修改 `gradle/libs.versions.toml`，添加 Kotlin 2.0.0、Compose BOM 2024.09.00、Compose Material3 等依赖版本
2. 修改 `app/build.gradle.kts`，应用 `org.jetbrains.kotlin.plugin.compose` 插件，启用 `buildFeatures { compose = true }`
3. 添加 Compose UI、Material3、Material Icons、Activity Compose 等依赖
4. `MainActivity` 改造为菜单页，提供 5 个按钮分别跳转到 5 个布局实验

### 3.3 实验一：线性布局（LinearLayout）

**要求**：利用线性布局实现 4×4 网格表格界面，黑色背景，白色文字居中。

**实现方式**：
- 使用垂直 `LinearLayout` 作为根容器
- 内嵌 4 个水平 `LinearLayout`，每行 4 个 `TextView`
- 通过 `layout_weight="1"` 实现等宽等高
- 单元格内容依次为 "One,One" 到 "Four,Four"

### 3.4 实验二：表格布局（TableLayout）

**要求**：利用表格布局实现菜单列表界面。

**实现方式**：
- 使用 `TableLayout` 作为根容器，`stretchColumns="*"` 拉伸所有列
- 标题行 "Hello TableLayout" 使用 `layout_span="2"` 跨两列
- 菜单项包括 Open、Save、Save As、Import、Export、Quit，右侧对应快捷键
- 行间使用 `<View>` 绘制灰色分隔线

### 3.5 实验三：约束布局1（计算器）

**要求**：利用 ConstraintLayout 实现计算器界面。

**实现方式**：
- 顶部 teal 色标题栏显示 "ConstraintLayoutTest"
- "Input" 标签 + 浅黄色显示框显示 "0.0"
- 4×4 按钮网格（7 8 9 + / 4 5 6 * / 1 2 3 + / . 0 = -）
- 使用 ConstraintLayout 的 chain（链）特性实现按钮等宽排列
- 每个按钮通过 `layout_constraintBottom_toTopOf`、`layout_constraintEnd_toStartOf` 形成水平和垂直链

### 3.6 实验四：约束布局2（太空旅行）

**要求**：利用 ConstraintLayout 实现太空旅行规划界面，图片资源从群文件下载。

**实现方式**：
- 顶部 Tab 栏：Space Stations | Flights | Rovers
- DCA 和 MARS 两个绿色圆角按钮，中间用 `double_arrows.png` 双向箭头图标
- One Way、1 Traveller 两个橙色按钮
- 中间区域：`rocket_icon.png` 火箭图标 + `galaxy.png` 星系图片
- 底部全宽绿色 DEPART 按钮
- 所有图片资源放置在 `res/drawable/` 目录

### 3.7 实验五：Compose 任务列表

**要求**：使用 Jetpack Compose 实现任务列表界面。

**实现方式**：
- 使用 `TaskListActivity.kt`（Kotlin）继承 `ComponentActivity`，调用 `setContent` 渲染 Compose UI
- 标题 "课程学习任务"（红色粗体）
- `OutlinedTextField` 输入框 + 红色 "添加" 按钮
- 状态文字 "已完成：X / Y" 实时更新
- `LazyColumn` 渲染任务列表，每项包含：复选框（`IconButton` + `CheckBox` 图标）、任务文字、删除按钮
- 初始 3 项任务，第 1 项已完成（带删除线）
- 添加任务后总数自动增加，勾选任务后完成数和删除线同步变化
- 使用 `mutableStateListOf` 管理任务列表状态，实现响应式 UI

## 四、项目结构

```
app/src/main/java/com/example/myfirstapp/
├── MainActivity.java              // 菜单页，跳转到各实验
├── LinearLayoutActivity.java      // 实验一：线性布局
├── TableLayoutActivity.java       // 实验二：表格布局
├── CalculatorActivity.java        // 实验三：计算器
├── SpaceTravelActivity.java       // 实验四：太空旅行
└── compose/
    └── TaskListActivity.kt        // 实验五：Compose任务列表

app/src/main/res/layout/
├── activity_main.xml              // 菜单页布局
├── activity_linear_layout.xml     // 4×4网格
├── activity_table_layout.xml      // 菜单列表
├── activity_calculator.xml        // 计算器
└── activity_space_travel.xml       // 太空旅行

app/src/main/res/drawable/
├── double_arrows.png              // 双向箭头
├── galaxy.png                     // 星系图
├── rocket_icon.png                // 火箭图标
├── rover_icon.png                 // 漫游车图标
├── single_arrow.png               // 单向箭头
└── space_station_icon.png         // 空间站图标
```

## 五、核心代码

### 5.1 MainActivity.java（菜单页）

```java
findViewById(R.id.btn_linear).setOnClickListener(v ->
        startActivity(new Intent(this, LinearLayoutActivity.class)));
findViewById(R.id.btn_table).setOnClickListener(v ->
        startActivity(new Intent(this, TableLayoutActivity.class)));
findViewById(R.id.btn_calculator).setOnClickListener(v ->
        startActivity(new Intent(this, CalculatorActivity.class)));
findViewById(R.id.btn_space).setOnClickListener(v ->
        startActivity(new Intent(this, SpaceTravelActivity.class)));
findViewById(R.id.btn_compose).setOnClickListener(v ->
        startActivity(new Intent(this, TaskListActivity.class)));
```

### 5.2 线性布局核心（activity_linear_layout.xml）

```xml
<LinearLayout android:orientation="vertical" android:background="#000000">
    <LinearLayout android:layout_height="0dp" android:layout_weight="1"
                  android:orientation="horizontal">
        <TextView android:layout_width="0dp" android:layout_weight="1"
                  android:gravity="center" android:text="One, One"
                  android:textColor="#FFFFFF"/>
        <!-- 其余单元格... -->
    </LinearLayout>
    <!-- 其余行... -->
</LinearLayout>
```

### 5.3 表格布局核心（activity_table_layout.xml）

```xml
<TableLayout android:stretchColumns="*">
    <TableRow android:background="#EEEEEE">
        <TextView android:layout_span="2" android:text="Hello TableLayout"/>
    </TableRow>
    <TableRow>
        <TextView android:text="Open..."/>
        <TextView android:text="Ctrl-O" android:gravity="end"/>
    </TableRow>
    <!-- 其余菜单项... -->
</TableLayout>
```

### 5.4 约束布局-计算器核心（activity_calculator.xml）

```xml
<Button android:id="@+id/btn_7"
    android:layout_width="0dp" android:layout_height="0dp"
    app:layout_constraintBottom_toTopOf="@id/btn_4"
    app:layout_constraintEnd_toStartOf="@id/btn_8"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toBottomOf="@id/display" />
<!-- 其余按钮通过链式约束排列... -->
```

### 5.5 Compose 任务列表核心（TaskListActivity.kt）

```kotlin
val tasks = remember {
    mutableStateListOf(
        TaskItem(1, "学习 Column 和 Row", completed = true),
        TaskItem(2, "学习状态管理", completed = false),
        TaskItem(3, "完成 Compose 实验", completed = false)
    )
}
val completedCount = tasks.count { it.completed }

LazyColumn {
    items(tasks) { task ->
        Row {
            IconButton(onClick = { /* 切换完成状态 */ }) {
                Icon(if (task.completed) Icons.Filled.CheckBox
                     else Icons.Filled.CheckBoxOutlineBlank)
            }
            Text(task.text, textDecoration =
                if (task.completed) TextDecoration.LineThrough else TextDecoration.None)
            TextButton(onClick = { /* 删除任务 */ }) { Text("删除") }
        }
    }
}
```

## 六、实验结果

1. **线性布局**：成功实现 4×4 网格表格，黑色背景白色文字，单元格等宽等高
2. **表格布局**：成功实现菜单列表，标题行跨列，菜单项与快捷键左右对齐
3. **约束布局1**：成功实现计算器界面，4×4 按钮通过 ConstraintLayout 链等宽排列
4. **约束布局2**：成功实现太空旅行界面，图片资源正确显示，DCA/MARS 按钮和 DEPART 按钮布局合理
5. **Compose 任务列表**：成功实现响应式任务列表，支持添加、勾选、删除任务，完成数和删除线实时同步
6. **编译通过**：BUILD SUCCESSFUL，37 个任务全部执行成功
7. **GitHub 推送成功**：仓库地址 https://github.com/yx0331-mobile/MyFirstApp

## 七、问题与解决

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| Kotlin Android 插件冲突 | AGP 9.3.0 已内置 Kotlin 支持，重复应用插件 | 移除 `org.jetbrains.kotlin.android`，仅保留 `org.jetbrains.kotlin.plugin.compose` |
| `kotlinOptions` 未解析引用 | AGP 9.x 移除了旧的 kotlinOptions DSL | 直接移除 kotlinOptions 配置块，由 AGP 自动处理 |
| GitHub 连接被重置 | DNS 被污染，github.com 无法解析 | 修改 hosts 文件添加 GitHub IP 映射，刷新 DNS 缓存后推送成功 |
| Compose 依赖下载缓慢 | 需要从 Maven Central 下载新依赖 | 调整仓库顺序，优先使用可访问的镜像，耐心等待下载完成 |

## 八、实验心得

通过本次实验，系统学习了 Android 五种主要布局方式。线性布局适合简单的等分排列场景；表格布局适合菜单、表单类界面；约束布局功能最强大，能实现复杂界面且性能优于嵌套布局；Jetpack Compose 作为 Google 推荐的声明式 UI 框架，通过状态驱动 UI 更新，代码更简洁。

实验中遇到的最大挑战是网络问题导致的依赖下载困难和 GitHub 推送失败。通过调整 Maven 仓库顺序解决了依赖下载问题，通过修改 hosts 文件解决了 GitHub DNS 污染问题。此外，AGP 9.x 的变化（内置 Kotlin、移除 kotlinOptions）也带来了一些配置上的调整。

这次实验让我认识到，Android 布局有多种实现方式，需要根据界面需求选择最合适的布局。同时，Jetpack Compose 代表了 Android UI 开发的未来方向，掌握声明式 UI 思维非常重要。
