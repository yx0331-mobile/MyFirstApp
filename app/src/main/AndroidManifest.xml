<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFirstApp">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LinearLayoutActivity"
            android:exported="false"
            android:label="线性布局" />

        <activity
            android:name=".TableLayoutActivity"
            android:exported="false"
            android:label="表格布局" />

        <activity
            android:name=".CalculatorActivity"
            android:exported="false"
            android:label="约束布局-计算器" />

        <activity
            android:name=".SpaceTravelActivity"
            android:exported="false"
            android:label="约束布局-太空旅行" />

        <activity
            android:name=".compose.TaskListActivity"
            android:exported="false"
            android:label="Compose任务列表" />
    </application>

</manifest>
